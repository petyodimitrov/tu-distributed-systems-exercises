version: '2'
services:
  sample_gateway-postgresql:
    image: postgres:11.3
    # volumes:
    #     - ~/volumes/jhipster/sample_gateway/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sample_gateway
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
